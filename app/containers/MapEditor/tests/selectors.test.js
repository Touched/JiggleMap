// import { fromJS } from 'immutable';
// import { makeSelectMapEditorDomain } from '../selectors';

// const selector = makeSelectMapEditorDomain();

describe('makeSelectMapEditorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
